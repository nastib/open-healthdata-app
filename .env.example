
# Supabase Project Configuration (Get these from your Supabase project settings)
# Use your self-hosted instance URL
SUPABASE_URL="Your Supabase URL" # e.g., https://supabase.digitlab.app

# Use the anon key for client-side access
SUPABASE_KEY="YOUR_ANON_KEY"

# Use the service role key for server-side/admin access (keep secure!)
SUPABASE_SERVICE_KEY="YOUR_SERVICE_ROLE_KEY"
DATABASE_PW="YOUR_DATABASE_PASSWORD"

# Database Connection (Used by Prisma)
# Ensure this points to your Supabase Postgres instance
DATABASE_URL="YOUR_DATABASE_URL" # e.g., postgres://

# Google OAuth Credentials (Get these from Google Cloud Console)
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"

# Nuxt Supabase Module Redirect URLs (Adjust as needed)
# NUXT_PUBLIC_SUPABASE_REDIRECT_OPTIONS_LOGIN=/login
# NUXT_PUBLIC_SUPABASE_REDIRECT_OPTIONS_CALLBACK=/confirm
# NUXT_PUBLIC_SUPABASE_REDIRECT_OPTIONS_EXCLUDE=/* # Example: Exclude all routes initially if handling manually

# Base URL for deployment (useful for OAuth redirects)
BASE_URL="YOUR_BASE_URL" # Change for production

# FormKit Pro Key (if using FormKit Pro features)
FORMKIT_PRO_KEY="YOUR_FORMKIT_PRO_KEY"
